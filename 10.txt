// --- 1. Global Variables & Mock Data ---
const categories = [
    { name: "Design Tools", icon: "fa-solid fa-pen-nib", count: 120 },
    { name: "AI Tools", icon: "fa-solid fa-robot", count: 85 },
    { name: "Fonts", icon: "fa-solid fa-font", count: 340 },
    { name: "Mockups", icon: "fa-solid fa-layer-group", count: 210 },
];

const resources = [
    { id: 1, name: "Figma", category: "design", desc: "Collaborative interface design tool.", rating: "4.9", img: "https://images.unsplash.com/photo-1611162617474-5b21e879e113?w=500&q=80" },
    { id: 2, name: "ChatGPT", category: "ai", desc: "Advanced AI language model for ideation.", rating: "4.8", img: "https://images.unsplash.com/photo-1677442136019-21780ecad995?w=500&q=80" },
    { id: 3, name: "Unsplash", category: "design", desc: "Beautiful, free images and photos.", rating: "4.9", img: "https://images.unsplash.com/photo-1550745165-9bc0b252726f?w=500&q=80" },
    { id: 4, name: "Midjourney", category: "ai", desc: "AI image generator from text prompts.", rating: "4.7", img: "https://images.unsplash.com/photo-1680795456480-1e5b4b10b005?w=500&q=80" },
];

// --- 2. Core Initializations ---
document.addEventListener('DOMContentLoaded', () => {
    // Hide Loader
    setTimeout(() => {
        document.getElementById('loader').style.opacity = '0';
        setTimeout(() => document.getElementById('loader').style.display = 'none', 500);
    }, 800);

    renderCategories();
    renderResources('all');
    initScrollReveal();
    initCounters();
});

// --- 3. DOM Manipulations & Rendering ---
function renderCategories() {
    const container = document.querySelector('.category-grid');
    container.innerHTML = categories.map(cat => `
        <div class="cat-card glass-card">
            <i class="${cat.icon}"></i>
            <h3>${cat.name}</h3>
            <p class="text-muted">${cat.count}+ Resources</p>
        </div>
    `).join('');
}

function renderResources(filterType) {
    const container = document.getElementById('resource-container');
    const filtered = filterType === 'all' ? resources : resources.filter(r => r.category === filterType);
    
    container.innerHTML = filtered.map(res => `
        <div class="res-card glass-card" onclick="openModal(${res.id})">
            <img src="${res.img}" alt="${res.name}">
            <div class="res-header">
                <span class="res-badge">${res.category.toUpperCase()}</span>
                <span class="text-muted"><i class="fa-solid fa-star" style="color: #F59E0B"></i> ${res.rating}</span>
            </div>
            <h3>${res.name}</h3>
            <p>${res.desc}</p>
            <button class="btn btn-primary w-full">View Details</button>
        </div>
    `).join('');
}

// --- 4. Interactive Features ---

// Theme Toggle
const themeBtn = document.getElementById('theme-toggle');
themeBtn.addEventListener('click', () => {
    const html = document.documentElement;
    const currentTheme = html.getAttribute('data-theme');
    const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
    html.setAttribute('data-theme', newTheme);
    themeBtn.innerHTML = newTheme === 'dark' ? '<i class="fa-solid fa-moon"></i>' : '<i class="fa-solid fa-sun"></i>';
});

// Category Filtering
document.querySelectorAll('.filter-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
        document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
        e.target.classList.add('active');
        renderResources(e.target.dataset.filter);
    });
});

// Scroll Reveal
function initScrollReveal() {
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('active');
            }
        });
    }, { threshold: 0.1 });

    reveals.forEach(reveal => observer.observe(reveal));
}

// Animated Counters
function initCounters() {
    const counters = document.querySelectorAll('.counter');
    const observer = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
            if(entry.isIntersecting) {
                const target = +entry.target.getAttribute('data-target');
                let count = 0;
                const updateCount = () => {
                    const inc = target / 50; // speed
                    if (count < target) {
                        count += inc;
                        entry.target.innerText = Math.ceil(count);
                        setTimeout(updateCount, 30);
                    } else {
                        entry.target.innerText = target;
                    }
                };
                updateCount();
                obs.unobserve(entry.target); // Run once
            }
        });
    }, { threshold: 0.5 });
    
    counters.forEach(counter => observer.observe(counter));
}

// Modal Logic
const modal = document.getElementById('resource-modal');
const closeBtn = document.querySelector('.close-modal');

function openModal(id) {
    const res = resources.find(r => r.id === id);
    document.getElementById('modal-body').innerHTML = `
        <img src="${res.img}" style="width:100%; border-radius:8px; margin-bottom: 20px;">
        <h2>${res.name}</h2>
        <p style="margin: 10px 0; color: var(--text-muted);">${res.desc}</p>
        <button class="btn btn-primary" style="margin-top: 15px;">Visit Official Website <i class="fa-solid fa-arrow-up-right-from-square"></i></button>
    `;
    modal.style.display = 'block';
}

closeBtn.onclick = () => modal.style.display = 'none';
window.onclick = (e) => { if (e.target == modal) modal.style.display = 'none'; }

// Back to Top functionality
const bttBtn = document.getElementById('back-to-top');
window.onscroll = () => {
    if (document.body.scrollTop > 300 || document.documentElement.scrollTop > 300) {
        bttBtn.style.display = "block";
    } else {
        bttBtn.style.display = "none";
    }
};
bttBtn.onclick = () => { window.scrollTo({top: 0, behavior: 'smooth'}); };